[workspace]

[package]
name = "fable_cosmo"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "fableScalar and fable cosmologies: quintessence-like scalar and 16-component spinor fields, integrated with the vendored pure-Rust SUNDIALS 7.8.0 CVODE of the rustSolveIt repositories (BDF + Newton + dense)."

[dependencies]
sundials_core = { path = "../vendor/rustSolveIt/sundials_rs/crates/sundials_core" }
cvode_rs = { path = "../vendor/rustSolveIt/sundials_rs/crates/cvode_rs" }

[profile.release]
opt-level = 3
